# This file contains all available configuration options
# with their default values.

# options for analysis running
run:
  go: '1.19'

  # timeout for analysis, e.g. 30s, 5m, default is 1m
  deadline: 10m

  # exit code when at least one issue was found, default is 1
  issues-exit-code: 1

  # include test files or not, default is true
  tests: true

  # which dirs to skip: they won't be analyzed;
  # can use regexp here: generated.*, regexp is applied on full path;
  # default value is empty list, but next dirs are always skipped independently
  # from this option's value:
  #   	vendor$, third_party$, testdata$, examples$, Godeps$, builtin$
  skip-dirs:

  # which files to skip: they will be analyzed, but issues from them
  # won't be reported. Default value is empty list, but there is
  # no need to include all autogenerated files, we confidently recognize
  # autogenerated files. If it's not please let us know.
  skip-files:
    - configure_core.go

  # whether to hide "congrats" message if no issues were found,
  # default is false (show "congrats" message by default).
  # set this option to true to print nothing if no issues were found.
  silent: true


# output configuration options
output:
  # colored-line-number|line-number|json|tab|checkstyle, default is "colored-line-number"
  format: line-number

  # print lines of code with issue, default is true
  print-issued-lines: true

  # print linter name in the end of issue text, default is true
  print-linter-name: true

linters:
  enable-all: true
  disable:
    - lll
    - funlen
    - testpackage
    - wsl
    - gci
    - goerr113
    - gochecknoglobals
    - nlreturn
    - wrapcheck
    - cyclop
    - varnamelen
    - errname
    - ifshort
    - interfacer
    - godot
    - dupl
    - maintidx
  fast: false

linters-settings:
  dupl:
    threshold: 200
  govet:
    # report about shadowed variables
    check-shadowing: true
  gocognit:
    min-complexity: 70
  gocyclo:
    min-complexity: 60
  errcheck:
    check-blank: true
    exclude: .golangci.errcheck.excludes.txt
    check-type-assertions: true
  exhaustive:
    # check switch statements in generated files also
    check-generated: false
    # indicates that switch statements are to be considered exhaustive if a
    # 'default' case is present, even if all enum members aren't listed in the
    # switch
    default-signifies-exhaustive: false
  exhaustivestruct:
    struct-patterns:
#      - '*.Context'
#      - '*.HTTPClient'

issues:
  exclude:
    - Context, HTTPClient are missing in
    - ID, CreatedAt are missing
    - CreatedAt, LastUpdatedAt are missing

    - Suite, ServiceName, Logger are missing in CoreSuite
    - are missing in PaymentSuite
    - actors, lock are missing in Group
    - CreatedAt, LastUpdatedAt are missing
    - String, Valid are missing in NullString
    - Time, Valid are missing in NullTime
    - Int64, Valid are missing in NullInt64
    - are missing in PlayerClaims
    - Handler returns interface
    - Transport, CheckRedirect, Jar are missing in Client
    - missing in DepositRequestAttributes
    - missing in WithdrawalRequestAttributes
    - are missing in PaymentiqTransaction
    - are missing in PaymentIQVerifyuserResponse
    - are missing in AccountTransaction
    - are missing in Deposit
    - are missing in Account
    - are missing in CreditDepositRequest
    - are missing in Withdraw
    - is missing in AccountTransaction
    - are missing in VeriffKycDecision
    - are missing in VeriffDecision
    - are missing in VeriffEvent
    - is missing in Message
    - RejectionReason is missing in WithdrawRejectedV1
    - FaultedReason is missing in WithdrawFaultedV1
    - are missing in Payment
    - Slug is missing in Game
    - should be DBDB
    - Decimal, Valid are missing in NullDecimal
    - XMLName is missing in AuthenticateSP
    - are missing in GlobalInputData
    - are missing in GlobalPersonal
    - are missing in GlobalPersonalDetails
    - are missing in GlobalAddresses
    - are missing in GlobalAddress